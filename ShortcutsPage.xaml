
<Page x:Class="Reports.ShortcutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="clr-namespace:Reports.Controls"
      mc:Ignorable="d"
      FlowDirection="RightToLeft"
      Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    
    <!-- Outer padding via Border -->
    <Border Padding="16">
        <Grid x:Name="RootGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Vertical" Margin="0,0,0,8">
                <TextBlock Text="העתקה מהירה"
                           FontSize="20"
                           FontWeight="Bold"/>
                <TextBlock Text="בחר קיצור כדי להעתיק תבנית מוכנה ללוח"
                           Opacity="0.8"/>
            </StackPanel>

            <!-- Content Card -->
            <ui:Card Grid.Row="1" Padding="8" IsEnabled="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <!-- Optional small hint row -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                        <ui:SymbolIcon Symbol="ClipboardCheckmark24" Margin="0,0,8,0"/>
                        <TextBlock Text="לחיצה אחת מעתיקה את התבנית ללוח" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Shortcuts grid -->
                    <UniformGrid Grid.Row="2" Columns="2">
                        <!-- GoTo: paid fine -->
                        <ui:Button  Appearance="Info"
                                   Tag="goto"
                                   FontWeight="SemiBold"
                                   Click="Paid_Click"
                                   Margin="0 6">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="ClipboardTaskAdd24" Margin="0,0,4,0"/>
                                <TextBlock Text="דוח שולם – גוטו"/>
                            </StackPanel>
                        </ui:Button>

                        <!-- Autotel: paid fine -->
                        <ui:Button Appearance="Info"
                                   Background="DarkOrange"
                                   Tag="autotel"
                                   FontWeight="SemiBold"
                                   MouseOverBackground="Orange"
                                   PressedBackground="#FFA65D"
                                   Click="Paid_Click">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="ClipboardTaskAdd24" Margin="0,0,4,0"/>
                                <TextBlock Text="דוח שולם – אוטותל"/>
                            </StackPanel>
                        </ui:Button>
                        <ui:Button Appearance="Info"
                                   Background="#0E7490"
                                   Tag="all"
                                   FontWeight="SemiBold"
                                   MouseOverBackground="#1198B8"
                                   PressedBackground="#14B7D6"
                                   Click="Transport_Click">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="ClipboardTaskAdd24" Margin="0,0,4,0"/>
                                <TextBlock Text="הסבת תחב״צ"/>
                            </StackPanel>
                        </ui:Button>
                    </UniformGrid>
                </Grid>
            </ui:Card>
            <controls:OverlayBanner x:Name="Overlay"
                                    Grid.Row="0"/>
        </Grid>
    </Border>
</Page>
